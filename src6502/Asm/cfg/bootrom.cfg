MEMORY {
    ZP:   start =   $20, size =   $1C, define = yes, file = "";
    RAM0: start =  $400, size = $0600, define = yes;
    RAM1: start = $1000, size = $b000, define = yes;
    CARD: start = $c000, size = $0800, define = yes;
    RAM2: start = $c900, size = $0600, define = yes;
    DEV:  start = $d000, size = $0100, define = yes;
    RAM3: start = $d100, size = $1f00, define = yes;
    ROM0: start = $f000, size = $0100, define = yes;
    ROM1: start = $f100, size = $0100, define = yes;
}

SEGMENTS {
    ZEROPAGE:  load = ZP, type = zp, define = yes;
    HEADER:    load = ROM0, type = ro;
    ONCE:      load = ROM0, type = ro, optional = yes;
    CODE:      load = ROM0, type = ro;
    STARTUP:   load = ROM0, type = ro, define = yes;
    RODATA:    load = ROM0, type = ro;
    DATA:      load = ROM0, run = RAM0, type = rw, define = yes;
    BSS:       load = RAM1, type = bss, define = yes;
}

FEATURES {
    CONDES: type    = constructor,
            label   = __CONSTRUCTOR_TABLE__,
            count   = __CONSTRUCTOR_COUNT__,
            segment = STARTUP;
    CONDES: type    = destructor,
            label   = __DESTRUCTOR_TABLE__,
            count   = __DESTRUCTOR_COUNT__,
            segment = RODATA;
    CONDES: type    = interruptor,
            label   = __INTERRUPTOR_TABLE__,
            count   = __INTERRUPTOR_COUNT__,
            segment = RODATA;
}

SYMBOLS {
    __STACKSIZE__:  value = $0200, type = weak;
    __CARDIO__: value = __CARD_START__+$800, type = weak;
}
